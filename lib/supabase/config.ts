// This file is no longer needed since we removed Supabase
// Keeping it for reference but it's not used anywhere
export const supabaseConfig = {
  url: "mock://localhost",
  anonKey: "mock-key",
}

console.warn("Supabase has been replaced with mock data for development.")
